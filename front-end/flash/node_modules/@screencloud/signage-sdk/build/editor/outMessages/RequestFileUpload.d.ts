import { IMessage } from "@screencloud/app-core";
import { SignageFile } from "../..";
export declare namespace RequestFileUpload {
    interface Payload {
        mimeTypes?: string | string[];
    }
    interface Response {
        error?: string;
        files?: SignageFile[];
    }
    type Type = "requestFileUpload";
    const type: Type;
    type Message = IMessage<Payload, Type>;
    type Handler = () => Promise<Response>;
}
