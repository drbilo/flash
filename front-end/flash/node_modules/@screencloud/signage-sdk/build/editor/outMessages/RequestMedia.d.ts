import { IMessage } from "@screencloud/app-core";
import { SignageFile } from "../..";
export declare namespace RequestMedia {
    interface Payload {
        mimeTypes: string | string[];
        multiSelect: boolean;
        folderSelect: boolean;
    }
    interface Response {
        media: SignageFile[];
    }
    type Type = "requestMedia";
    const type: Type;
    type Message = IMessage<Payload, Type>;
    type Handler = (payload: Payload) => Promise<Response>;
}
