import { IMessage } from "@screencloud/app-core";
import { IAppConfig } from "../..";
export declare namespace RequestConfigUpdate {
    interface Response {
        config: IAppConfig;
    }
    interface Payload {
        type: "preview" | "save";
    }
    type Type = "requestConfigUpdate";
    const type: Type;
    type Message = IMessage<Payload, Type>;
    type Handler = (payload: Payload) => Promise<Response>;
}
