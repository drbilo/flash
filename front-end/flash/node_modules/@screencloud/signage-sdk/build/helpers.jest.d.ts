import { IBridge } from "@screencloud/app-core";
export declare function getMethodsData(prefix: string, reflectObj: object): Array<{
    name: string;
    expectsPayload: boolean;
    type: string;
}>;
export declare const fakeBridge: IBridge;
