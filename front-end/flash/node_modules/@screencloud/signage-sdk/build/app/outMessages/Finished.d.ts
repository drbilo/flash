import { IMessage } from "@screencloud/app-core";
export declare namespace Finished {
    interface Payload {
        error?: {
            code: string | number;
            message?: string;
        };
    }
    type Type = "finished";
    const type: Type;
    type Message = IMessage<Payload, Type>;
    type Handler = (payload?: Payload) => void;
}
