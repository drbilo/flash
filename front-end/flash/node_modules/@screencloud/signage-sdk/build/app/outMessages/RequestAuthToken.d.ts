import { IMessage } from "@screencloud/app-core";
export declare namespace RequestAuthToken {
    interface Payload {
        audience: string;
    }
    interface Response {
        authToken: string;
        expiresAt: number;
    }
    type Type = "requestAuthToken";
    const type: Type;
    type Message = IMessage<Payload, Type>;
    type Handler = (payload: Payload) => Promise<Response>;
}
