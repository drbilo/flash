import { IMessage } from "@screencloud/app-core";
import { UUID } from "@screencloud/uuid";
import { IAppState } from "../..";
export declare namespace StateUpdated {
    interface Payload {
        screenId?: UUID;
        state: IAppState;
    }
    type Type = "stateUpdated";
    const type: Type;
    type Message = IMessage<Payload, Type>;
    type Handler = (payload: Payload) => void;
}
