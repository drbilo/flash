import { IMessage } from "@screencloud/app-core";
import { UUID } from "@screencloud/uuid";
import { IAppConfig, IAppPlayerContext, IAppState } from "../..";
export declare namespace Initialize {
    interface Payload {
        appId: UUID;
        appInstanceId: UUID;
        authority: string;
        config: IAppConfig;
        context: IAppPlayerContext;
        orgId: UUID;
        teamId: UUID;
        screenId?: UUID;
        userId?: UUID;
        state: IAppState;
    }
    type Type = "initialize";
    const type: Type;
    type Message = IMessage<Payload, Type>;
    type Handler = (payload: Payload) => void;
}
